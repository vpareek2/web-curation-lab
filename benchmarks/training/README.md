# Training benchmarks

This directory records the measurements used to select the model architecture
and training configuration. Each result should include the TorchTitan commit,
hardware, software versions, model shape, sequence length, batch sizes,
precision, compile settings, tokens per second, MFU, and peak memory.
